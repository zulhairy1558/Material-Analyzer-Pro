# Suppress deprecation warnings from transitive dependencies (exceljs → archiver → glob, etc.)
# These are internal to exceljs and cannot be upgraded until exceljs updates its dependencies.
loglevel=error
